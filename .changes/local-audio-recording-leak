patch type="fixed" "The microphone no longer keeps recording after a call ends: the audio device module recording started when a local audio track starts capture is now released when the last one stops, stopping a track while its start is still in flight now tears the capture down instead of returning early, and an iOS audio session deactivation rejected as busy is now retried instead of leaving the microphone indicator lit"
